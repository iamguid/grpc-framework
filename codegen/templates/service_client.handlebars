// GENERATED CODE -- DO NOT EDIT!

import * as grpcWeb from "grpc-web";
{{#each imports}}
import * as {{this.name}} from "{{this.path}}";
{{/each}}

{{#each clients}}
export interface {{this.interfaceClassName}} {
    {{#each this.methods}}
    {{#if this.method.isServerStreaming}}
    {{this.methodName}}: (request: {{this.inputType}}, metadata: grpcWeb.Metadata) => grpcWeb.ClientReadableStream<{{this.outputType}}>
    {{else}}
    {{this.methodName}}: (request: {{this.inputType}}, metadata: grpcWeb.Metadata) => Promise<{{this.outputType}}>
    {{/if}}
    {{/each}}
}

export class {{this.clientClassName}} implements {{this.interfaceClassName}} {
    {{#each this.methods}}
    public {{this.methodName}}(request: {{this.inputType}}, metadata: grpcWeb.Metadata): Promise<{{this.outputType}}> {
        return void;
    }
    {{/each}}
}
{{/each}}
